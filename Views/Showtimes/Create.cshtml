@model CinemaManagement.Models.Showtime
@{
    ViewData["Title"] = "Thêm suất chiếu";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

<style>
    .form-wrapper {
        max-width: 600px;
        margin: 50px auto;
    }

    .custom-card {
        background-color: #1c1c1c;
        color: #fff;
        border-radius: 10px;
        box-shadow: 0 0 15px rgba(255, 0, 0, 0.3);
    }

    .custom-header {
        background-color: #000;
        padding: 20px;
        text-align: center;
        border-radius: 10px 10px 0 0;
    }

        .custom-header h3 {
            color: #ff4d4d;
            margin: 0;
        }

    .form-label {
        color: #ff4d4d;
        font-weight: bold;
    }

    .form-control, .form-select {
        background-color: #2c2c2c;
        color: white;
        border: 1px solid #ff4d4d;
    }

        .form-control::placeholder, .form-select::placeholder {
            color: #bbbbbb;
        }

    .btn-red {
        background-color: #ff4d4d;
        color: #fff;
        border: none;
    }

        .btn-red:hover {
            background-color: #e60000;
        }

    .btn-outline-light {
        border: 1px solid #fff;
        color: #fff;
    }

        .btn-outline-light:hover {
            background-color: #fff;
            color: #000;
        }

    .text-danger {
        color: #ff6666 !important;
    }
</style>

<div class="form-wrapper">
    <div class="custom-card">
        <div class="custom-header">
            <h3><i class="bi bi-plus-circle"></i> Thêm suất chiếu cho phim: <strong>@ViewBag.MovieTitle</strong></h3>
        </div>
        <div class="p-4">
            @if (!ViewData.ModelState.IsValid && ViewData.ModelState.Values.SelectMany(v => v.Errors).Any())
            {
                <div class="alert alert-danger">
                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <p>@error.ErrorMessage</p>
                    }
                </div>
            }

            <form asp-action="Create" method="post">
                <input type="hidden" asp-for="MovieId" value="@ViewBag.MovieId" />

                <div class="mb-3">
                    <label for="TheaterSelect" class="form-label">🏛 Chọn rạp</label>
                    <select id="TheaterSelect" class="form-select">
                        <option value="">-- Chọn rạp --</option>
                        @foreach (var theater in ViewBag.Theaters as List<CinemaManagement.Models.Theater>)
                        {
                            <option value="@theater.TheaterId">@theater.Name</option>
                        }
                    </select>
                </div>

                <div class="mb-3">
                    <label asp-for="RoomId" class="form-label">🚪 Chọn phòng</label>
                    <select asp-for="RoomId" class="form-select" id="RoomSelect">
                        <option value="">-- Chọn phòng --</option>
                    </select>
                    <span asp-validation-for="RoomId" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="StartTime" class="form-label">🕒 Thời gian bắt đầu</label>
                    <input asp-for="StartTime" type="datetime-local" class="form-control" placeholder="Chọn thời gian bắt đầu..." />
                    <span asp-validation-for="StartTime" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Price" class="form-label">💰 Giá tiền</label>
                    <input asp-for="Price" class="form-control" type="number" step="1" min="1" max="99999999" placeholder="Nhập giá tiền..." />
                    <span asp-validation-for="Price" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="DiscountPercent" class="form-label">🎁 Giảm giá (%)</label>
                    <input asp-for="DiscountPercent" class="form-control" placeholder="Nhập phần trăm giảm giá..." />
                    <span asp-validation-for="DiscountPercent" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="DiscountStart" class="form-label">📅 Từ ngày</label>
                    <input asp-for="DiscountStart" type="datetime-local" class="form-control" placeholder="Chọn ngày bắt đầu..." />
                    <span asp-validation-for="DiscountStart" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="DiscountEnd" class="form-label">📅 Đến ngày</label>
                    <input asp-for="DiscountEnd" type="datetime-local" class="form-control" placeholder="Chọn ngày kết thúc..." />
                    <span asp-validation-for="DiscountEnd" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <button type="submit" class="btn btn-red px-4">
                        <i class="bi bi-save"></i> Thêm suất chiếu
                    </button>
                    <a asp-action="Index" class="btn btn-outline-light px-4">
                        <i class="bi bi-arrow-left"></i> Quay lại
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        $(document).ready(function () {
            $('#TheaterSelect').change(function () {
                var theaterId = $(this).val();
                var roomSelect = $('#RoomSelect');
                roomSelect.empty().append('<option>Đang tải phòng...</option>');

                if (theaterId) {
                    $.getJSON('/Showtimes/GetRoomsByTheater?theaterId=' + theaterId, function (data) {
                        roomSelect.empty().append('<option value="">-- Chọn phòng --</option>');
                        $.each(data, function (i, room) {
                            roomSelect.append($('<option>', {
                                value: room.roomId,
                                text: room.name
                            }));
                        });
                    });
                } else {
                    roomSelect.empty().append('<option value="">-- Chọn rạp trước --</option>');
                }
            });
        });
    </script>
}