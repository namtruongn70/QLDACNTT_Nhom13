@model CinemaManagement.ViewModel.MovieFormViewModel
@{
    ViewData["Title"] = "Chỉnh sửa phim";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

<style>
    .form-wrapper {
        max-width: 600px;
        margin: 50px auto;
    }

    .custom-card {
        background-color: #1c1c1c;
        color: #fff;
        border-radius: 10px;
        box-shadow: 0 0 15px rgba(255, 0, 0, 0.3);
    }

    .custom-header {
        background-color: #000;
        padding: 20px;
        text-align: center;
        border-radius: 10px 10px 0 0;
    }

        .custom-header h3 {
            color: #ff4d4d;
            margin: 0;
        }

    .form-label {
        color: #ff4d4d;
        font-weight: bold;
    }

    .form-control {
        background-color: #2c2c2c;
        color: white;
        border: 1px solid #ff4d4d;
    }

        .form-control::placeholder {
            color: #bbbbbb;
        }

    .btn-red {
        background-color: #ff4d4d;
        color: #fff;
        border: none;
    }

        .btn-red:hover {
            background-color: #e60000;
        }

    .btn-outline-light {
        border: 1px solid #fff;
        color: #fff;
    }

        .btn-outline-light:hover {
            background-color: #fff;
            color: #000;
        }

    .text-danger {
        color: #ff6666 !important;
    }

    .form-check-input {
        background-color: #2c2c2c;
        border: 1px solid #ff4d4d;
    }

        .form-check-input:checked {
            background-color: #ff4d4d;
            border-color: #ff4d4d;
        }

        .form-check-input:focus {
            box-shadow: 0 0 0 3px rgba(255, 77, 77, 0.3);
        }

    .btn-outline-success {
        border: 1px solid #28a745;
        color: #28a745;
    }

        .btn-outline-success:hover {
            background-color: #28a745;
            color: #fff;
        }

    .btn-outline-primary {
        border: 1px solid #007bff;
        color: #007bff;
    }

        .btn-outline-primary:hover {
            background-color: #007bff;
            color: #fff;
        }

    hr {
        border-color: #ff4d4d;
    }
</style>

<div class="form-wrapper">
    <div class="custom-card">
        <div class="custom-header">
            <h3><i class="bi bi-pencil-square"></i> Chỉnh sửa phim</h3>
        </div>
        <div class="p-4">
            @if (!ViewData.ModelState.IsValid && ViewData.ModelState.Values.SelectMany(v => v.Errors).Any())
            {
                <div class="alert alert-danger">
                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <p>@error.ErrorMessage</p>
                    }
                </div>
            }

            <form asp-action="Edit" method="post">
                <input type="hidden" asp-for="Movie.MovieId" />

                <div class="mb-3">
                    <label asp-for="Movie.Title" class="form-label">🎬 Tên phim</label>
                    <input asp-for="Movie.Title" class="form-control" placeholder="Nhập tên phim..." />
                    <span asp-validation-for="Movie.Title" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Movie.Description" class="form-label">📝 Mô tả phim</label>
                    <textarea asp-for="Movie.Description" class="form-control" placeholder="Nhập mô tả phim..." rows="3"></textarea>
                    <span asp-validation-for="Movie.Description" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Movie.Duration" class="form-label">⏱ Thời lượng (phút)</label>
                    <input asp-for="Movie.Duration" class="form-control" placeholder="Nhập thời lượng..." />
                    <span asp-validation-for="Movie.Duration" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Movie.PosterUrl" class="form-label">🖼 URL Poster</label>
                    <input asp-for="Movie.PosterUrl" class="form-control" placeholder="Nhập URL poster..." />
                    <span asp-validation-for="Movie.PosterUrl" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Movie.VideoUrl" class="form-label">🎥 URL Video</label>
                    <input asp-for="Movie.VideoUrl" class="form-control" placeholder="Nhập URL video..." />
                    <span asp-validation-for="Movie.VideoUrl" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Movie.ReleaseDate" class="form-label">📅 Ngày phát hành</label>
                    <input asp-for="Movie.ReleaseDate" class="form-control" type="date" />
                    <span asp-validation-for="Movie.ReleaseDate" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label class="form-label">🎭 Thể loại</label>
                    <div class="d-flex flex-wrap gap-3">
                        @foreach (var genre in Model.Genres)
                        {
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" name="SelectedGenres" value="@genre.GenreId"
                                @(Model.SelectedGenres.Contains(genre.GenreId) ? "checked" : "") />
                                <label class="form-check-label" style="color: #fff;">@genre.Name</label>
                            </div>
                        }
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label">🏛 Rạp chiếu</label>
                    <div class="d-flex flex-wrap gap-3">
                        @foreach (var theater in Model.Theaters)
                        {
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" name="SelectedTheaterId" value="@theater.TheaterId"
                                @(Model.SelectedTheaterId.Contains(theater.TheaterId) ? "checked" : "") />
                                <label class="form-check-label" style="color: #fff;">@theater.Name</label>
                            </div>
                        }
                    </div>
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <button type="submit" class="btn btn-red px-4">
                        <i class="bi bi-save"></i> Cập nhật
                    </button>
                    <a asp-action="Index" class="btn btn-outline-light px-4">
                        <i class="bi bi-arrow-left"></i> Quay lại
                    </a>
                </div>
            </form>

            <hr class="my-4" />

            @if (User.IsInRole("Admin"))
            {
                <div class="text-end">
                    <a asp-controller="Showtimes" asp-action="Create" asp-route-movieId="@Model.Movie.MovieId" class="btn btn-outline-success me-2 px-4">
                        <i class="bi bi-plus-circle"></i> Thêm suất chiếu mới
                    </a>
                    <a asp-controller="Showtimes" asp-action="Index" asp-route-movieId="@Model.Movie.MovieId" class="btn btn-outline-primary px-4">
                        <i class="bi bi-film"></i> Quản lý suất chiếu
                    </a>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}