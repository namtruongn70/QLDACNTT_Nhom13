@{
    ViewData["Title"] = "Xem phim";
}

<div class="container text-center">
    <h2 class="mt-4">🎬 @ViewBag.MovieTitle</h2>
    <p><strong>Thể loại:</strong> @ViewBag.MovieGenre | <strong>Ngày phát hành:</strong> @ViewBag.ReleaseDate</p>
    <p><strong>Thời lượng:</strong> @ViewBag.MovieDuration phút</p>

    <div class="mt-3">
        <iframe width="80%" height="450"
                src="@ViewBag.MovieEmbedUrl"
                frameborder="0"
                allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
                allowfullscreen>
        </iframe>
    </div>

    <a href="@Url.Action("Index", "Movies")" class="btn btn-primary mt-3">⬅ Quay lại danh sách phim</a>
</div>
3">⬅ Quay lại danh sách phim</a>
</div>
